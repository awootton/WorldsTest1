
Each if these is supposed to represent a standalone app as best possible. 

So, for example, only ONE of them is expected to register to receive events when loaded as an iFrame.

Each one should be considered to act like a separate iFrame.


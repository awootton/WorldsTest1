## The most importantant demo 

would be where an rpc is sent, for instance, with a blob/GLB in it to "the mainland" and then the player at gotohere.can display that content. The top of TheFormallyOrange-4west.tsx got pretty close to setting all that up. The rpc gadget is set up so it got pretty close.

Unfortunately, I'm not working. It would be great if you could make some progress sometime. 

This is a 'server' for 6 metaspace-proto spaces.
It's on port 3010 

start with ```yarn start``` 

build with ```yarn build``` and that makes the static files.

The script in ./build_to_s3_docs.sh requires an aws account. 

Follow progress on The Metaspace project on X @alan-tracey-wootton



This is a 'server' for  metaspace-proto spaces.
It's on port 3010 

start with ```yarn start```

build with ```yarn build``` and that makes the static files.

The script in ./build_to_s3_docs.sh won't work for you. 

follow progress on The Metaspace project on X @alan-tracey-wootton

this? 
git remote add origin https://github.com/awootton/WorldsTest1
or this?
git remote add origin git@github.com:awootton/WorldsTest1.git

